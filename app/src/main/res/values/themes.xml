<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.My_Pokedex" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.My_Pokedex" parent="Base.Theme.My_Pokedex" />
    <!-- Tema para la Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- El color de fondo de la pantalla de bienvenida -->
        <item name="windowSplashScreenBackground">@android:color/holo_blue_bright</item>
        <!-- El icono que se mostrará (nuestra pokeball) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_adapted</item>
        <!-- El tema que se usará DESPUÉS de que termine la bienvenida -->
        <item name="postSplashScreenTheme">@style/Theme.My_Pokedex</item>
    </style>

</resources>